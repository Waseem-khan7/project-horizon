export type PersonalInfo = {
  name: string;
  role: string;
  employeeId: string;
  email: string;
  phone: string;
  location: string;
  department: string;
  joined: string;
};

export type AccountInfo = {
  username: string;
  workEmail: string;
  technology: string;
  timezone: string;
};

export type Activity = {
  id: number;
  title: string;
  date: string;
};
